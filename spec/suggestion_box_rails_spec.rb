require "spec_helper"

describe SuggestionBoxRails do
  it "has a version number" do
    expect(SuggestionBoxRails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
